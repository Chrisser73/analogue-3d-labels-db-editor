services:
  a3d-labelsdb-editor:
    image: nginx:alpine
    container_name: a3d-labelsdb-editor
    restart: unless-stopped
    volumes:
      - ./:/usr/share/nginx/html:ro
    ports:
      - "4377:80"
